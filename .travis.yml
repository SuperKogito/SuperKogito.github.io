language: bash
python:
  - "3.6"
before_script:
  - sudo pip install --upgrade pip
  - sudo pip install Sphinx
  - sudo pip install sphinxcontrib-packages
  - sudo pip install git+git://github.com/SuperKogito/sphinxcontrib-pdfembed
  - sudo pip install sphinxcontrib-googleanalytics
  - sudo pip install sphinxjp.themes.basicstrap
  - sudo pip install sphinx_bootstrap_theme
  - sudo apt-get install python-enchant
  - sudo pip install sphinxcontrib-spelling
  - sudo pip install sphinxcontrib-bibtex
  - sudo pip install -U ablog
  - sudo pip install sphinxcontrib-napoleon
  - sudo pip install sphinx-sitemap
  - cd _project/docs/

script:
  - ./quickBuild.sh

branches:
  only:
    - master

notifications:
  email: false
    
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: public
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  keep-history: true  
  on:
    branch: master
  repo: SuperKogito/SuperKogito.github.io
  target_branch: master
